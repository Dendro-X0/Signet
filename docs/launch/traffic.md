# Signet Phase 1 — Traffic (quality)

Full strategy mirror lives in strategy-research-lab `signet-phase1-traffic-plan.md`. This is the **in-repo** ops copy.

## Funnel

```text
Query → HOOK → demo/GIFT → sample TRACK → CONVERT → soft-no → STOP
```

## Five target queries (Windows-first)

1. Tauri/Electron SmartScreen — what to tell users  
2. Self-signed checksum / `signet verify`  
3. TRUST.md release checksum  
4. Integrity vs reputation code signing indie  
5. Sideload Windows — users don’t trust install  

One HOOK owns the story; satellites soft-link to HOOK. No Fiverr/lifetime traffic tools. No PH-as-strategy.

## Metrics

| ID | Metric | Success read |
|----|--------|--------------|
| M1 | Hook views | Vanity only |
| M2 | Demo starts | **Primary quality** |
| M3 | Sample opens | Trust without trial |
| M4 | Checkout starts | Intent |
| M5 | Paid | Pulse |
| M6 | Junk (bounce, zero demo) | Kill channel |

Distribute only after unlock: README soft CTA → SEO headings → opportunistic thread answers linking HOOK first.
