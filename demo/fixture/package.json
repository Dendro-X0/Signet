{
  "name": "hello-signet",
  "version": "0.0.0",
  "private": true,
  "description": "Demo marker only — not a runnable Electron app",
  "devDependencies": {
    "electron": "unused-marker-for-signet-scan"
  }
}
